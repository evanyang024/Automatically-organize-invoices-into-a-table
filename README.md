# 发票自动处理工具

自动识别中国增值税电子发票 PDF，提取关键字段并写入 Excel 台账。

---

## 功能

把发票 PDF 丢进 `input/`，自动完成：**文本提取 → 字段解析 → 智能重命名 → 去重 → 写入 Excel → 归档副本**

| 功能 | 说明 |
|------|------|
| PDF 识别 | 支持横排、竖排、散列、铁路客票 4 种格式 |
| 金额解析 | 价税合计 = max(¥金额)，三级回退，数字类型可求和 |
| 重命名 | `YYYY-MM-DD-项目-金额.pdf`，多明细合并为"等N项" |
| Excel | 3 列（货物名称 / 价税合计 / 开票日期），无边框，只追加 |
| 去重 | 按发票号码，独立跟踪文件 |
| 归档 | input/ 原文件不动，archive/ 存副本 |
| 对照 | 整理后自动核对 input/ ↔ archive/ |

---

## 快速开始

**环境：** Python 3.10+，Windows/macOS/Linux

```bash
git clone https://github.com/evanyang024/Automatically-organize-invoices-into-a-table.git
cd Automatically-organize-invoices-into-a-table
pip install pymupdf openpyxl watchdog pyyaml

# 放入发票 PDF 到 input/（首次运行自动创建 input/output/archive 三个文件夹）
python run.py --once

# 或持续监控，有发票放进去就自动处理：
python run.py
```

---

## 项目结构

```
├── run.py                    # 主入口
├── config.yaml               # 配置
├── input/                    # 放入发票
├── output/                   # Excel 台账
├── archive/                  # 归档副本
└── invoice_processor/
    ├── extractor.py          # PDF 提取
    ├── parser.py             # 字段解析（v7，含铁路客票）
    ├── renamer.py            # 重命名
    ├── writer.py             # Excel 写入
    └── watcher.py            # 文件夹监控
```

---

## 完整流程

```
放入 input/ → 提取文本 → 解析字段(正则) → 重命名(日期-项目-金额)
→ 去重(发票号) → 写 Excel(追加/数字/无边框) → 归档(copy 副本) → 对照检查
```

### 金额规则

发票上 ¥ 数字位置不固定，**价税合计永远取最大的 ¥ 金额**。

### Excel 格式

| 货物或服务名称 | 价税合计 | 开票日期 |
|------|------|------|
| 云服务费 | 71.00 | 06月03日 |
| 铁路客运 | 124.00 | 06月03日 |

- 价税合计为数字类型，可直接 SUM
- 无边框
- 日期格式 MM月DD日
- 多明细合并一行

---

## 不同用户

| 用户 | 使用方式 |
|------|----------|
| Hermes Agent | 复制 `skills/` 到 `~/.hermes/skills/productivity/` |
| Claude Code / Codex | 直接用项目代码，纯 Python 独立运行 |
| 通用 | 6 个核心文件，跨平台可用 |

---

## 平台

| 功能 | Windows | macOS | Linux |
|------|:--:|:--:|:--:|
| PDF 提取 | ✅ | ✅ | ✅ |
| 字段解析 | ✅ | ✅ | ✅ |
| 重命名 | ✅ | ✅ | ✅ |
| Excel 写入 | ✅ | ✅ | ✅ |
| 文件夹监控 | ✅ | ✅ | ✅ |
| Excel 锁自动处理 | ✅ | ❌ | ❌ |

---

## 自定义

修改 `config.yaml`：增减列、关闭重命名/去重/归档。修改 `parser.py` 适配新发票格式。

## 铁律

1. 不删原文件  2. 不改旧数据  3. 金额必须正确  4. 加新不丢旧  5. 整理完对照

---

## 版本更新日志

### v9.0.0 (2026-06-17)

**🚀 重复发票处理优化**
- 检测到重复发票时，同时跳过台账写入和归档
- 避免archive目录产生带时间戳后缀的冗余副本

**📋 新增票据类型支持**
- 中央非税收入统一票据（专利费等）
- 浙江省公路车辆通行费票据
- 铁路电子客票（12306火车票）

**🧹 项目名称清理**
- 自动去除星号标记
- 去除中英文括号及后面内容
- 关键词截断（沪房地、不动产权等）
- 去除末尾"无"字
- 非税收入票据数字前缀清理

**⚡ 解析器增强**
- 3级¥回退：同行¥ → 跨行¥ → (小写)附近 → 最大数字
- 铁路客票专用解析，避免身份证号误识别
- 开票日期与乘车日期区分

---

### v5.0.0 (初始版本)

**基础功能**
- 发票自动识别（横排/竖排格式）
- 智能重命名：`YYYY-MM-DD-项目-金额.pdf`
- Excel台账自动写入（3列：货物名称、价税合计、开票日期）
- 文件夹持续监控（watchdog）
- 按发票号码去重
- 自动归档到archive目录

---

**完整变更历史：** [Releases](https://github.com/evanyang024/Automatically-organize-invoices-into-a-table/releases)

### v10.0.0 (2026-07-06)

**🐛 Bug修复**
- 身份证号掩码误识别：`****1819`被B策略提取为"1819"，添加过滤逻辑跳过全由`*`和数字组成的匹配
- 铁路电子客票检测补全：添加`_parse_train_ticket()`函数，检测"电子发票（铁路电子客票）"标识
- 停车费映射："车辆"（去"无"后只剩"车辆"）→ 自动映射为"停车费"

**⚡ 解析器增强**
- B策略正则匹配时过滤身份证号掩码
- 铁路客票专用解析，项目名固定"铁路客运"

### v10.0.6 (2026-07-06)

**🔧 新增**
- 台账按项目类型分类排序
- 加油费统一命名
- 铁路客票优先取乘车日期
- 餐饮→餐饮费 / 住宿→住宿费统一命名
- 写入前自动保存关闭已打开的Excel/WPS（支持Microsoft Excel和WPS Office）：汽油/柴油/燃油 → 统一映射为"加油费"



### v10.0.9 (2026-09-03)

**🎯 全文语义命名（读全发票内容，不再断章取义）**

- 修复车费类发票命名不准确问题：
  - `临时`（临时停车）→ **停车费**
  - `代收高`（代收高速通行费）→ **高速通行费**
  - `车位`（车位场地占用）→ **停车费**
  - `车辆通行费` → **高速通行费**（统一归类）
- 核心：新增 `_classify_vehicle_charge()` 读全文特征判断，而非抓字段片段
  - 高速通行费：全文含`高速`+`通行费`+`入口/出口收费站`
  - 停车费：全文含`停车`/`车位`/`场地占用`/`停车场`
- 中文跨行项目名合并（如 `临时\n停车费` → `临时停车费`）

### v10.0.8 (2026-09-03)

**🚀 新增：递归扫描子文件夹 + 解压压缩包**

- `input/` 下不仅支持顶层 PDF，子文件夹里的 PDF 也会被递归收集处理
- 支持压缩包：`.zip` `.rar` `.7z`，自动解压并提取其中的 PDF 处理
- 压缩包处理后会归档一份副本到 `archive/` 再删除源压缩包
- 子文件夹里的原 PDF 按铁律"不删原文件"保留（归档副本在 archive/）
- 已处理发票清单见 `output/.processed_invoices.json`

### v10.0.7 (2026-09-03)

**🐛 Bug修复：ASCII项目名跨行被截断**
- 场景：如下发票 `*修理修配服务*LGD,CSOT` + 下一行 `水质分析仪维修`，完整项目名应为 `LGD,CSOT水质分析仪维修`
- 修复1：B策略正则 `(?:\s|$)` → `(?:\n|$)`，避免在空格处截断
- 修复2：新增 B2 合并逻辑——若项目名全为 ASCII 字符（如 `LGD,CSOT`），检查原文该名称后是否紧跟换行+中文行，有则拼接（如 `LGD,CSOT` + `水质分析仪维修`）
- 同步修复：技能 references/parser.py 同步到生产目录 v10.0.7 版本，确保技能文档与实际运行代码一致

